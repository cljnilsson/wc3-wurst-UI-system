package Selection

import Button
import RegisterEvents
import Generate

function onClick()
    unit u = GetTriggerUnit()
    integer id = u.getTypeId()
    if id == detector
        GetTriggerPlayer().clearSelection()
        hash.get(u).onClick(GetTriggerPlayer())

init
    registerPlayerUnitEvent(EVENT_PLAYER_UNIT_SELECTED, function onClick)